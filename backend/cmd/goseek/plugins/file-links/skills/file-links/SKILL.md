# file-links

修改或创建文件后给出可点击链接。当用户要求修改代码、创建文件时使用。

## 指令

1. 用 write_file 修改文件后，在最终回复中按以下格式列出改动：

   `修改了 [文件名](file:///workspace相对路径)`

   例如：`修改了 [tokens.go](file:///backend/internal/domain/tokens.go)`

2. 如果修改了多个文件，逐个列出，不要合并成一段话。
3. 如果用户需要查看改动内容，回复文件路径即可，GoSeek 会在前端侧边栏显示。
4. 不要给目录路径加链接，只给文件。
